\begin{question}{{ '{' }}{{ label }}{{ '}' }}
  {{ prompt }}

  \begin{responses}{
      {%- if instructor is defined -%}
        ({{ instructor.mean }},Instructor) +- ({{ instructor.error }},0)
      {%- endif -%}
      ({{ course.mean }},Course) +- ({{ course.error }},0)
      ({{ department.mean }},Department) +- ({{ department.error }},0)
      ({{ academy.mean }},Academy) +- ({{ academy.error }},0)
  }
{% if responses -%}
    \begin{enumerate}[
        nosep,
    ]
\item {{ responses | join('\n\item ') }}
    \end{enumerate}
{%- endif %}
  \end{responses}
\end{question}
